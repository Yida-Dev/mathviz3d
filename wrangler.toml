# Cloudflare Pages 配置（Wrangler）
#
# 说明：
# - `pages_build_output_dir` 指向前端静态产物目录（Vite 默认 dist）
# - `compatibility_date` 对 Pages 静态站点不是必需，但保留便于未来引入 Workers/Functions
name = "mathviz3d"
compatibility_date = "2026-01-30"
pages_build_output_dir = "dist"
